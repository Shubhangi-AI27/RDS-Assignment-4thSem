# Write an R script that creates a string containing a newline, tab, and a backslash using escape sequences. Print the
# string to the console using cat().

text <- "Line 1\nLine 2 with a tab:\tTabbed Text\nA backslash: \\"
cat(text)

# Output 
# Line 1
# Line 2 with a tab:	Tabbed Text
# A backslash: \
