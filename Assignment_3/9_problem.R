# Write R code that concatenates the strings "Data", "Science", and "R" into a single string with hyphens (-) as
# separators.

str1 <- "Data"
str2 <- "Science"
str3 <- "R"
concatenated_string <- paste(str1, str2, str3, sep = "-")
print(concatenated_string)

# Output
# [1] "Data-Science-R"
